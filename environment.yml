name: project-env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - requests
  - invoke
  - pip
  - pip:
      - airoh>=0.3.2
      - jupyter>=1.0
      - nbconvert>=7.0
      - matplotlib>=3.5
      - pandas>=1.3
